# AgriTwin AI — Python dependencies
# Install: pip install -r requirements.txt

streamlit>=1.35.0
plotly>=5.20.0
numpy>=1.26.0
pandas>=2.2.0

# AI providers — install only what you have a key for
google-generativeai>=0.7.0   # Gemini (free tier available)
openai>=1.30.0                # OpenAI (optional fallback)

# Utilities
python-dotenv>=1.0.0          # .env file support
